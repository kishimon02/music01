# Native bindings plan

This folder is reserved for pybind11 integration.

Planned exports:

- `AudioEngine.start(config)`
- `AudioEngine.stop()`
- `Project.create_track(...)`
- `Project.export_wav(...)`

Current repository only provides Python-side contracts and service stubs.
